const fs=require("fs"),assert=require("assert"); const s=fs.readFileSync("TarsReportApp.js","utf8"); const a=s.indexOf("async function shouldForwardConfirmedWorkPhoto"),b=s.indexOf("async function fastForwardPersonalReportPhotos",a); assert(a>=0&&b>a); const x=s.slice(a,b); assert(x.includes('dominantKind === "receipt"'),"primary receipt remains blocked"); assert(x.includes('dominantKind === "mailing"'),"primary mailing remains blocked"); assert(x.includes('dominantKind === "photo"'),"primary work photo remains final"); assert(x.includes('personalImageOcrFallbackKind'),"inconclusive primary results retain one financial/text fallback"); assert(!x.includes('requestOpenAiWorkPhotoCheck'),"a second Vision classifier must not run"); assert(!x.includes('validateReceiptStrict'),"full receipt extraction must not run inside work-photo classification"); assert(x.includes('primary-vision-inconclusive'),"unknown images remain blocked"); console.log("PASS: single-pass work-photo confirmation cannot bypass receipts or mailings");

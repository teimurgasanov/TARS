const fs=require("fs"),assert=require("assert"); const s=fs.readFileSync("TarsReportApp.js","utf8"); const a=s.indexOf("async function shouldForwardConfirmedWorkPhoto"),b=s.indexOf("async function fastForwardPersonalReportPhotos",a); assert(a>=0&&b>a); const x=s.slice(a,b); assert(x.includes('finalKind === "receipt"')); assert(x.includes('finalKind === "mailing"')); assert(!x.includes('stable-0944-unknown-photo-fallback')); assert(x.includes('requestOpenAiWorkPhotoCheck')); assert(x.includes('forward: false, reason: finalKind || "unknown"')); console.log("PASS: unknown personal images require dedicated work-photo confirmation");

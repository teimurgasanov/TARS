const fs=require("fs"),assert=require("assert"); const s=fs.readFileSync("TarsReportApp.js","utf8"); const a=s.indexOf("async function shouldForwardConfirmedWorkPhoto"),b=s.indexOf("async function fastForwardPersonalReportPhotos",a); assert(a>=0&&b>a); const x=s.slice(a,b); assert(x.includes('fallbackKind === "receipt"')); assert(x.includes('fallbackKind === "mailing"')); assert(!x.includes('stable-0944-unknown-photo-fallback')); assert(!x.includes('requestOpenAiWorkPhotoCheck')); assert(x.includes('primary-vision-inconclusive')); console.log("PASS: unknown personal images stop after the single OCR fallback");
